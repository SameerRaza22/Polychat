name : "PolyChat-build"
services:
  db:
    image : postgres:latest
    environment:
      POSTGRES_USER : "postgres"
      POSTGRES_PASSWORD : "postgres"
      POSTGRES_DB : "polychatbuild"
    ports:
      - 5432:5432
    restart : always